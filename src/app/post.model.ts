export class Post {
  constructor(public title: string, public description: string, public imagePath: string, public alt: string, public author: string,
              public dateTimeCreated: Date) {
  }
}
